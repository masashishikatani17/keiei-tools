# CLAUDE.md — 経営tools 実装方針

## プロジェクト概要

経営toolsは、小さな経営判断ツールを素早く公開して利用状況を確認する個人開発プロジェクトです。経営者向けに「経営の判断を、数字でもっと簡単に。」をコンセプトとした無料の静的Webサイトで、GitHub Pagesで公開します。

## 基本方針

- 機能を増やすことよりも、分かりやすさ、速さ、保守の簡単さを優先する。
- サーバー側処理が必要になるまでは静的サイト（HTML / CSS / Vanilla JavaScript）を維持する。
- ユーザー登録、データ保存、データベース、複雑なJavaScriptフレームワークは、明確な必要性が生じるまで導入しない。
- 1つのツールでは、原則として1つの経営判断だけを扱う。
- 「将来利用するかもしれない」という理由だけで機能や共通化を追加しない。
- 計算処理は可能な限り確定的な計算式で行い、AIを使用しない。
- ユーザーに入力された情報は、現在の仕様では外部へ送信・保存しない。
- アクセス解析、広告タグ、Cookieを利用する機能は現時点では追加しない。
- 外部JavaScriptライブラリ、CSSフレームワーク、CDN、外部Webフォントは原則使用しない。

## URL構成

```text
/           トップページ
/hiring/    採用判断シミュレーター
```

将来的に `/cash/` `/price-up/` `/loan/` `/investment/` などの別ツールを追加できる構成とするが、必要性が確認できたものだけを追加する。

## ファイル構成

```text
keiei-tools/
├── index.html          # トップページ
├── hiring/
│   └── index.html      # 採用判断シミュレーター
├── css/
│   └── style.css       # 全ページ共通スタイル
├── js/
│   └── hiring.js       # 採用判断シミュレーターの計算処理
├── README.md
└── CLAUDE.md
```

新しいツールを追加する場合は `ツール名/index.html` と `js/ツール名.js` を追加する。過剰にファイルを細分化しない。

## コーディング指針

- リンクやCSS/JSの参照は相対パスで書く（GitHub Pagesのプロジェクトページ配下でも動作させるため）。
- 金額の内部計算は円単位で正確に行い、主要な結果は1万円単位に四捨五入した「万円」で表示する。
- `NaN`、`Infinity`、JavaScriptの例外をそのまま画面に表示しない。
- バリデーションエラーは該当欄の近くに分かりやすい日本語で表示し、再計算時に古いエラーや古い結果を残さない。
- スマートフォンで横スクロールを発生させない。ボタンや入力欄は指で押しやすい大きさにする。
- 断定的な経営判断（「採用すべき」「危険」など）は表示せず、数字の目安の提示にとどめる。

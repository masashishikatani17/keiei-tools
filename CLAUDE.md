# CLAUDE.md — 経営tools 実装方針

## プロジェクト概要

経営toolsは、小さな経営判断ツールを素早く公開して利用状況を確認する個人開発プロジェクトです。経営者向けに「経営の判断を、数字でもっと簡単に。」をコンセプトとした無料の静的Webサイトで、GitHub Pages＋独自ドメイン（https://keiei-tools.jp/）で公開しています。

## 現在のフェーズ

検索流入がほぼゼロの段階では「1ツール公開して反応を待つ」戦略は機能しないため、**複数のツールを先に並べて検索の間口を広げ、Search Consoleのクエリ・表示回数で反応を確認する**フェーズにあります。反応が確認できたテーマから深掘り・改善します。ツールの数を増やす場合も、1ページごとの品質（1判断・分かりやすい説明・テスト済みの計算）は落とさないこと。

## 基本方針

- 機能を増やすことよりも、分かりやすさ、速さ、保守の簡単さを優先する。
- サーバー側処理が必要になるまでは静的サイト（HTML / CSS / Vanilla JavaScript）を維持する。
- ユーザー登録、データ保存、データベース、複雑なJavaScriptフレームワークは、明確な必要性が生じるまで導入しない。
- 1つのツールでは、原則として1つの経営判断だけを扱う。
- 「将来利用するかもしれない」という理由だけで機能や共通化を追加しない。
- 計算処理は可能な限り確定的な計算式で行い、AIを使用しない。
- ユーザーに入力された情報は、現在の仕様では外部へ送信・保存しない。
- 広告タグ、Cookieを利用する機能は追加しない。アクセス計測はCookieを使わないGoatCounter（keiei-tools.goatcounter.com）のみを利用し、入力値は計測に送信しない。
- 外部JavaScriptライブラリ、CSSフレームワーク、CDN、外部Webフォントは原則使用しない。唯一の例外はGoatCounterの計測スクリプト（https://gc.zgo.at/count.js を全ページ末尾で読み込む）。

## URL構成

```text
/                トップページ（ツール一覧）
/hiring/         採用判断シミュレーター
/breakeven/      損益分岐点シミュレーター
/target-profit/  目標利益シミュレーター
/price-up/       値上げシミュレーター
/discount/       値引きシミュレーター
/cash/           資金繰りシミュレーター
/loan/           借入返済シミュレーター
/investment/     投資回収シミュレーター
/cost-cut/       経費削減シミュレーター
/margin/         粗利益率計算ツール
```

## ファイル構成

```text
keiei-tools/
├── index.html          # トップページ
├── ツール名/
│   └── index.html      # 各ツールのページ
├── css/
│   └── style.css       # 全ページ共通スタイル
├── js/
│   ├── common.js       # 共通ヘルパー（window.KT）
│   └── ツール名.js      # 各ツールの計算処理
├── sitemap.xml         # 全ページを列挙（ページ追加時に更新する）
├── robots.txt
├── 404.html            # 自己完結スタイル（どの階層でも表示されるため）
├── favicon.svg         # サイトアイコン（各ページから相対パスで参照）
├── apple-touch-icon.png
├── og-image.png        # OGP共有画像（og:imageは絶対URLで参照）
├── CNAME               # GitHub Pagesの独自ドメイン設定（削除しない）
├── README.md
└── CLAUDE.md
```

新しいツールを追加する場合は `ツール名/index.html` と `js/ツール名.js` を追加し、トップページのカード一覧と `sitemap.xml` にも追記する。過剰にファイルを細分化しない。

例外: `/kuchikomi/` は税理士・会計事務所向け新サービス「口コミ作成アシスタント」の試作デモで、10ツールの原則（1経営判断・共通CSS/JS）とは別枠の自己完結ページ。noindexとし、sitemap.xmlには載せない。トップページの「開発中の新サービス」セクションからリンクする。

## 共通ヘルパー（js/common.js）

数値フォーマット（万円表示・カンマ区切り・％）、入力の読み取り・検証、エラー表示、粗利益率の補助計算（売上高・売上原価から計算して反映）は `window.KT` に集約している。ツール固有の計算式・結果表示は各ツールのJSに書く。共通化するのは「全ツールで実際に使っている処理」だけに限る。

## コーディング指針

- リンクやCSS/JSの参照は相対パスで書く（GitHub Pagesのプロジェクトページ配下でも動作させるため）。ただし `canonical` と OGP の `og:url` は `https://keiei-tools.jp/` の絶対URLで書く。
- 金額の内部計算は円単位で正確に行い、主要な結果は1万円単位に四捨五入した「万円」で表示する（KT.formatApproxYen）。
- `NaN`、`Infinity`、JavaScriptの例外をそのまま画面に表示しない。割り算の分母が0になり得る入力は、事前のバリデーションか条件分岐で必ず塞ぐ。
- バリデーションエラーは該当欄の近くに分かりやすい日本語で表示し、再計算時に古いエラーや古い結果を残さない。
- スマートフォンで横スクロールを発生させない。ボタンや入力欄は指で押しやすい大きさにする。
- 断定的な経営判断（「採用すべき」「危険」など）は表示せず、数字の目安の提示にとどめる。計算上の事実（「回収できない計算になります」など）は表示してよい。
- 各ツールに「この数字の見方」の説明領域を置き、専門用語をなるべく使わずに計算の意味を説明する。
- 実装後は必ずテストケース（手計算で検証済みの入力と期待値）で結果を確認してからコミットする。
- 新しいツールを追加したら、計算成功時に `KT.trackEvent('calc-ツール名')` を呼び、GoatCounterのイベントとして利用回数を記録する。
